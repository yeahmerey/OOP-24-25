package problem2;

public class Position {
	public char letter ; 
	public int number ; 
	
	public Position(char letter , int number) {
		if(letter >= 'a' && letter <='h') {
			this.letter = letter ; 
		}
		if(number >= 1 && number <= 8) {
			this.number = number ; 
		}
	}
}
