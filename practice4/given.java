package practice4;

public class given {
/*Practice 4: create an abstract class Shape with
*abstract method draw() and fields position(int),
*color (Color, enum). Then create concrete classes
*Circle, Rectange, Triangle that contain 
*implementation of draw() and have some
*specific information. In your test 
*class, put different 
**shape objects to Vector and draw them.*/
}
