package problem5;
import java.util.* ; 
public class PersonRegistry {
	public static Vector<Person> persons = new Vector<Person>(); 
	public PersonRegistry() {
	}
	
	static void addPerson(Person person) {
		persons.add(person); 
	}
	static void removePerson(Person person) {
		persons.remove(person); 
	}
	static void seePersons() {
		for(Person person : persons) {
			System.out.println(person.toString()); 
		}
	}
	
	/*
	  public static Vector<Person> findOwnersByPet(Animal pet){
	  Vector<Person> owners = new Vector<>(); 
	  for(Person person : persons){
	  	if(person.hasPet(pet){
	  		owners.add(person); 
	  }
	  for(Person owner : owners){
	  	System.out.println(owner.getName()); 
	  }
	  */
}
